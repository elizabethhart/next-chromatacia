// 3rd Party
import type { NextPage } from 'next';
import { serverSideTranslations } from 'next-i18next/serverSideTranslations';
import Head from 'next/head';
import { FaGithub } from 'react-icons/fa';
import { FiMail } from 'react-icons/fi';
import { MdConstruction } from 'react-icons/md';

// UI
import PageContent from './components/PageContent';

const Home: NextPage = () => {
  return (
    <>
      <Head>
        <title>Chromatacia | Home</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      {process.env.NODE_ENV === 'development' ? (
        <PageContent>
          <div className="w-full flex flex-row justify-center">
            <div className="text-center">HOME</div>
          </div>
        </PageContent>
      ) : (
        <div className="bg-slate-900 absolute top-0 bottom-0 left-0 right-0 text-stone-300">
          <div className="w-full h-full flex flex-col justify-center items-center">
            <h1 className="text-4xl mb-2">Welcome!</h1>
            <p>This site is currently under construction.</p>
            <div className="mt-10 p-10 w-60 flex flex-row justify-between border-stone-300 border-2 rounded bg-slate-800">
              <a href="https://github.com/elizabethhart">
                <FaGithub size="30px" />
              </a>
              <a href="mailto:elizabethwhart@gmail.com">
                <FiMail size="30px" />
              </a>
            </div>
          </div>
        </div>
      )}
    </>
  );
};

export async function getStaticProps({ locale }: { locale: string }) {
  return {
    props: {
      ...(await serverSideTranslations(locale, ['common'])),
      // Will be passed to the page component as props
    },
  };
}

export default Home;
